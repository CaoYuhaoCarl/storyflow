## Pattern library

We use the adk-kit at `~/Desktop/wikix/raw/adk-kit/`.

When asked to build any new ADK agent:
1. First read `~/Desktop/wikix/raw/adk-kit/recipes/README.md`.
2. Pick the closest recipe; read that file.
3. Read only the units it imports.
4. Compose into this project's `agent.py`.

Never reinvent. Never wrap. Copy snippets verbatim, then change instructions/schemas.
