[project]
name = "english-coach"
version = "0.2.0"
description = "ADK v2 workflow for routed English coaching feedback and training inputs from ./input/."
requires-python = ">=3.10"
dependencies = [
    "google-adk",
    "pydantic>=2",
]
